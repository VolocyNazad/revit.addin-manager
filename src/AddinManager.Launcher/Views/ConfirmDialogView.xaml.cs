namespace AddinManager.Launcher.Views;

/// <summary>Вид окна подтверждения: карточка в стиле приложения вместо системного MessageBox.</summary>
public partial class ConfirmDialogView
{
    /// <summary>Создает вид (контекст подставляет композиция через AddModule).</summary>
    public ConfirmDialogView()
    {
        InitializeComponent();
    }
}
