<Application
    x:Class="AddinManager.Launcher.App"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Application.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="pack://application:,,,/PresentationFramework.Fluent;component/Themes/Fluent.xaml" />
                <ResourceDictionary Source="Resources/Icons.xaml" />
            </ResourceDictionary.MergedDictionaries>
            <DrawingImage x:Key="ApplicationIcon">
                <DrawingImage.Drawing>
                    <GeometryDrawing Brush="#45CDFF" Geometry="M19,4 C25.627,4 31,9.373 31,16 C31,22.627 25.627,28 19,28 C13.793,28 9.362,24.683 7.701,20.047 C6.692,21.241 5.185,22 3.5,22 C2.599,22 1.75,21.781 1,21.398 C2.78,20.487 4,18.637 4,16.5 C4,14.363 2.781,12.511 1,11.601 C1.75,11.217 2.6,11 3.5,11 C5.059,11 6.465,11.65 7.465,12.691 C8.901,7.673 13.521,4 19,4 M19,9 C15.134,9 12,12.134 12,16 C12,19.866 15.134,23 19,23 C22.866,23 26,19.866 26,16 C26,12.134 22.866,9 19,9 M22,11 C23.657,11 25,12.343 25,14 C25,15.657 23.657,17 22,17 C20.343,17 19,15.657 19,14 C19,12.343 20.343,11 22,11 M23,12 C22.448,12 22,12.448 22,13 C22,13.552 22.448,14 23,14 C23.552,14 24,13.552 24,13 C24,12.448 23.552,12 23,12" />
                </DrawingImage.Drawing>
            </DrawingImage>
            <Style x:Key="SearchBoxStyle" TargetType="TextBox">
                <Setter Property="Padding" Value="0,7" />
                <Setter Property="BorderThickness" Value="1" />
                <Setter Property="BorderBrush" Value="{DynamicResource CaptionHoverBrush}" />
                <Setter Property="Background" Value="Transparent" />
                <Setter Property="Foreground" Value="{DynamicResource AppForegroundBrush}" />
                <Setter Property="CaretBrush" Value="{DynamicResource AppForegroundBrush}" />
                <Setter Property="FontSize" Value="12" />
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="TextBox">
                            <Border
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="9">
                                <ScrollViewer
                                    x:Name="PART_ContentHost"
                                    Margin="12,0"
                                    VerticalAlignment="Center"
                                    HorizontalScrollBarVisibility="Disabled"
                                    VerticalScrollBarVisibility="Disabled" />
                            </Border>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>
            <Style x:Key="FieldTextBoxStyle" TargetType="TextBox">
                <Setter Property="Padding" Value="10,7" />
                <Setter Property="BorderThickness" Value="1" />
                <Setter Property="BorderBrush" Value="{DynamicResource CaptionHoverBrush}" />
                <Setter Property="Background" Value="Transparent" />
                <Setter Property="Foreground" Value="{DynamicResource AppForegroundBrush}" />
                <Setter Property="CaretBrush" Value="{DynamicResource AppForegroundBrush}" />
                <Setter Property="FontSize" Value="12" />
                <Setter Property="IsReadOnly" Value="{Binding IsLocked}" />
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="TextBox">
                            <Border
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="9">
                                <ScrollViewer
                                    x:Name="PART_ContentHost"
                                    Margin="{TemplateBinding Padding}"
                                    VerticalAlignment="Center"
                                    HorizontalScrollBarVisibility="Disabled"
                                    VerticalScrollBarVisibility="Disabled" />
                            </Border>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>
            <Style x:Key="CardToolTipStyle" TargetType="ToolTip">
                <Setter Property="HasDropShadow" Value="True" />
                <Setter Property="Placement" Value="Right" />
                <Setter Property="HorizontalOffset" Value="8" />
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="ToolTip">
                            <Border
                                Padding="12,10"
                                Background="{DynamicResource AppBackgroundBrush}"
                                BorderBrush="{DynamicResource CaptionHoverBrush}"
                                BorderThickness="1"
                                CornerRadius="10"
                                TextElement.Foreground="{DynamicResource AppForegroundBrush}">
                                <ContentPresenter />
                            </Border>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>
            <Style x:Key="WarningCardToolTipStyle" TargetType="ToolTip">
                <Setter Property="HasDropShadow" Value="True" />
                <Setter Property="Placement" Value="Right" />
                <Setter Property="HorizontalOffset" Value="8" />
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="ToolTip">
                            <Border
                                Padding="12,10"
                                Background="{DynamicResource WarningBackgroundBrush}"
                                BorderBrush="{DynamicResource WarningBorderBrush}"
                                BorderThickness="1"
                                CornerRadius="10"
                                TextElement.Foreground="{DynamicResource AppForegroundBrush}">
                                <ContentPresenter />
                            </Border>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>
            <Style x:Key="TooltipFieldLabelStyle" TargetType="TextBlock">
                <Setter Property="FontSize" Value="11" />
                <Setter Property="Foreground" Value="{DynamicResource TooltipLabelBrush}" />
                <Setter Property="Margin" Value="0,0,10,4" />
                <Setter Property="TextWrapping" Value="Wrap" />
            </Style>
            <Style x:Key="TooltipFieldValueStyle" TargetType="TextBlock">
                <Setter Property="FontSize" Value="11" />
                <Setter Property="Foreground" Value="{DynamicResource AppForegroundBrush}" />
                <Setter Property="Margin" Value="0,0,0,4" />
                <Setter Property="TextWrapping" Value="Wrap" />
            </Style>

            <Style x:Key="FieldLabelStyle" TargetType="TextBlock">
                <Setter Property="FontSize" Value="11" />
                <Setter Property="Opacity" Value="0.55" />
                <Setter Property="Margin" Value="0,0,0,4" />
            </Style>
            <Style x:Key="FieldGroupStyle" TargetType="StackPanel">
                <Setter Property="Margin" Value="0,0,0,8" />
            </Style>


            <Style x:Key="RowContainerStyle" TargetType="ListBoxItem">
                <Setter Property="Padding" Value="0" />
                <Setter Property="HorizontalContentAlignment" Value="Stretch" />
                <Setter Property="FocusVisualStyle" Value="{x:Null}" />
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="ListBoxItem">
                            <ContentPresenter />
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>
            <Style x:Key="LayoutIconButton" TargetType="Button">
                <Setter Property="Background" Value="Transparent" />
                <Setter Property="BorderThickness" Value="0" />
                <Setter Property="Padding" Value="6,0,6,0" />
                <Setter Property="Height" Value="24" />
                <Setter Property="Foreground" Value="{DynamicResource AppForegroundBrush}" />
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="Button">
                            <Border
                                Padding="{TemplateBinding Padding}"
                                Background="{TemplateBinding Background}"
                                CornerRadius="4">
                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
                            </Border>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
                <Style.Triggers>
                    <Trigger Property="IsMouseOver" Value="True">
                        <Setter Property="Background" Value="{DynamicResource CaptionHoverBrush}" />
                    </Trigger>
                </Style.Triggers>
            </Style>
            <!--
                Чекбокс пакетного выбора (строки списка и записей): родной Fluent-шаблон
                не следит за нашей темой, поэтому свой минималистичный бокс на токенах —
                рамка/заливка AppForegroundBrush, галка Icon.Check поверх (ей сюда
                передаём AppBackgroundBrush — на заливке нужна контрастная).
            -->
            <Style x:Key="SelectionCheckBoxStyle" TargetType="CheckBox">
                <Setter Property="Cursor" Value="Hand" />
                <Setter Property="FocusVisualStyle" Value="{x:Null}" />
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="CheckBox">
                            <Grid
                                Width="16"
                                Height="16"
                                Background="Transparent">
                                <Border
                                    x:Name="Box"
                                    Background="Transparent"
                                    BorderBrush="{DynamicResource AppForegroundBrush}"
                                    BorderThickness="1.5"
                                    CornerRadius="4"
                                    Opacity="0.6" />
                                <ContentControl
                                    x:Name="Glyph"
                                    Width="16"
                                    Height="16"
                                    HorizontalAlignment="Center"
                                    VerticalAlignment="Center"
                                    Focusable="False"
                                    Foreground="{DynamicResource AppBackgroundBrush}"
                                    Template="{StaticResource Icon.Check}"
                                    Visibility="Collapsed" />
                            </Grid>
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsChecked" Value="True">
                                    <Setter TargetName="Box" Property="Background" Value="{DynamicResource AppForegroundBrush}" />
                                    <Setter TargetName="Box" Property="Opacity" Value="1" />
                                    <Setter TargetName="Glyph" Property="Visibility" Value="Visible" />
                                </Trigger>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter TargetName="Box" Property="Opacity" Value="1" />
                                </Trigger>
                                <Trigger Property="IsEnabled" Value="False">
                                    <Setter TargetName="Box" Property="Opacity" Value="0.3" />
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>
            <!--
                Раскрытие чекбокса: Collapsed — место не резервируется, строки
                сдвигаются при наведении; виден по наведению на строку или пока
                есть выбор. Одинаково для списка файлов и записей: обе модели отдают
                HasSelection, строка выделяется через ListBoxItem.IsMouseOver.
            -->
            <Style
                x:Key="SelectionCheckBoxRevealStyle"
                BasedOn="{StaticResource SelectionCheckBoxStyle}"
                TargetType="CheckBox">
                <Setter Property="Visibility" Value="Collapsed" />
                <Style.Triggers>
                    <DataTrigger Binding="{Binding RelativeSource={RelativeSource AncestorType=ListBoxItem}, Path=IsMouseOver}" Value="True">
                        <Setter Property="Visibility" Value="Visible" />
                    </DataTrigger>
                    <DataTrigger Binding="{Binding RelativeSource={RelativeSource AncestorType=ListBox}, Path=DataContext.HasSelection}" Value="True">
                        <Setter Property="Visibility" Value="Visible" />
                    </DataTrigger>
                </Style.Triggers>
            </Style>
            <!--
                Родной Fluent-скроллбар не следит за нашей темой — перешаблонен на свои токены.
                Width/Height и IsDirectionReversed обязаны различать Orientation: иначе
                ломается горизонтальный скролл редактора (уже было однажды, см. architecture.md).
            -->
            <Style x:Key="MinimalScrollBarThumbStyle" TargetType="Thumb">
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="Thumb">
                            <Border
                                x:Name="ThumbVisual"
                                Background="{DynamicResource AppForegroundBrush}"
                                CornerRadius="3"
                                Opacity="0.25" />
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter TargetName="ThumbVisual" Property="Opacity" Value="0.5" />
                                </Trigger>
                                <Trigger Property="IsDragging" Value="True">
                                    <Setter TargetName="ThumbVisual" Property="Opacity" Value="0.6" />
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>
            <Style TargetType="ScrollBar">
                <Setter Property="Background" Value="Transparent" />
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="ScrollBar">
                            <Grid Background="{TemplateBinding Background}">
                                <Track x:Name="PART_Track" IsDirectionReversed="True">
                                    <Track.DecreaseRepeatButton>
                                        <RepeatButton
                                            Command="ScrollBar.PageUpCommand"
                                            Focusable="False"
                                            IsTabStop="False"
                                            Opacity="0" />
                                    </Track.DecreaseRepeatButton>
                                    <Track.IncreaseRepeatButton>
                                        <RepeatButton
                                            Command="ScrollBar.PageDownCommand"
                                            Focusable="False"
                                            IsTabStop="False"
                                            Opacity="0" />
                                    </Track.IncreaseRepeatButton>
                                    <Track.Thumb>
                                        <Thumb
                                            x:Name="PART_Thumb"
                                            Margin="3,1"
                                            Style="{StaticResource MinimalScrollBarThumbStyle}" />
                                    </Track.Thumb>
                                </Track>
                            </Grid>
                            <ControlTemplate.Triggers>
                                <Trigger Property="Orientation" Value="Horizontal">
                                    <Setter TargetName="PART_Track" Property="IsDirectionReversed" Value="False" />
                                    <Setter TargetName="PART_Thumb" Property="Margin" Value="1,3" />
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
                <Style.Triggers>
                    <Trigger Property="Orientation" Value="Vertical">
                        <Setter Property="Width" Value="10" />
                    </Trigger>
                    <Trigger Property="Orientation" Value="Horizontal">
                        <Setter Property="Height" Value="10" />
                    </Trigger>
                </Style.Triggers>
            </Style>
        </ResourceDictionary>
    </Application.Resources>
</Application>


