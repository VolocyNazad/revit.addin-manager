<Window x:Class="AddinManager.Playground.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Playground" Width="1080" Height="720">
  <Grid>
    <Grid.RowDefinitions>
      <RowDefinition Height="Auto" />
      <RowDefinition Height="*" />
      <RowDefinition Height="Auto" />
    </Grid.RowDefinitions>

    <Button Grid.Row="0" Margin="12,12,12,8" HorizontalAlignment="Left" Click="OnRender">Показать (Ctrl+Enter)</Button>

    <Grid Grid.Row="1" Margin="12,0,12,12">
      <Grid.ColumnDefinitions>
        <ColumnDefinition Width="*" />
        <ColumnDefinition Width="*" />
      </Grid.ColumnDefinitions>
      <TextBox x:Name="Source" Grid.Column="0" Margin="0,0,8,0"
               AcceptsReturn="True" AcceptsTab="True"
               FontFamily="Consolas" FontSize="12"
               VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Auto"
               PreviewKeyDown="OnSourceKeyDown" />
      <ContentControl x:Name="Preview" Grid.Column="1" />
    </Grid>

    <TextBlock x:Name="MessageText" Grid.Row="2" Margin="12,0,12,12" TextWrapping="Wrap" Visibility="Collapsed" />
  </Grid>
</Window>
